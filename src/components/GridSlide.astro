---
import '../styles/gridslide.css'
// ServicesSlider.astro
import image1 from '../assets/vertex4.webp';
import image2 from '../assets/vertex5.webp';
import image3 from '../assets/l3.webp';

const services = [
  {
    image: image1.src,
    title: 'Servicios de Transporte Marítimo',
    description: 'Soluciones integrales de transporte marítimo con alcance global'
  },
  {
    image: image2.src,
    title: 'Rastreo Global de Carga',
    description: 'Rastreo Global de Mercancía'
  },
  {
    image: image3.src,
    title: 'Soluciones Logísticas Eficientes',
    description: 'Entregas rápidas con la máxima seguridad garantizada'
  }
];
---
<section class="services-carousel-section">
  <div class="carousel-box">
    <button class="carousel-arrow carousel-prev" aria-label="Previous">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
        <path d="M15 18l-6-6 6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </button>

    <div class="carousel-viewport">
      <div class="carousel-slides">
        <!-- Clones al final para loop infinito -->
        {services.map(service => (
          <div class="carousel-item clone">
            <img src={service.image} alt={service.title} class="carousel-image" />
            <div class="carousel-gradient"></div>
            <div class="carousel-text">
              <h2>{service.title}</h2>
              <p>{service.description}</p>
            </div>
          </div>
        ))}
        <!-- Slides originales -->
        {services.map(service => (
          <div class="carousel-item">
            <img src={service.image} alt={service.title} class="carousel-image" />
            <div class="carousel-gradient"></div>
            <div class="carousel-text">
              <h2>{service.title}</h2>
              <p>{service.description}</p>
            </div>
          </div>
        ))}
        <!-- Clones al inicio para loop infinito -->
        {services.map(service => (
          <div class="carousel-item clone">
            <img src={service.image} alt={service.title} class="carousel-image" />
            <div class="carousel-gradient"></div>
            <div class="carousel-text">
              <h2>{service.title}</h2>
              <p>{service.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <button class="carousel-arrow carousel-next" aria-label="Next">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
        <path d="M9 18l6-6-6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </button>
  </div>
</section>


<script>
    import '../scripts/gridslide'
</script>